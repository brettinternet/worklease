---
id: TASK-129.6
title: Benchmark the queue against the section 14 budgets
status: Done
assignee:
  - '@brett'
created_date: '2026-09-23 04:29'
updated_date: '2026-09-24 21:25'
labels:
  - work-queue
  - reviewed
milestone: m-1
dependencies:
  - TASK-129.1
  - TASK-129.3
  - TASK-129.4
  - TASK-129.5
references:
  - mise.toml
documentation:
  - docs/work-queue-tui-proposal.md
parent_task_id: TASK-129
priority: high
type: task
ordinal: 26000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Plan section 14 sets acceptance budgets as prototype exit criteria: validate them or revise them with recorded measurements before scope expands, and never weaken safety to meet them. Measure on the D22 reference machine (Apple M1 Max, 32 GiB) with fixed fixtures and injected network latency. CI tracks relative regressions only, never absolute numbers.

Reuse the TASK-126.6 Backlog.md fixture generator, and add a GitHub fixture served by the fake GitHub with configurable latency.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Reproducible benchmark fixtures exist for 10,000-summary Backlog.md and GitHub sources, a 50,000-summary multi-source view, and a 100,000-summary stress view, with injected network latency
- [x] #2 p50, p95, and p99 are recorded, together with process count, API request count, bytes transferred, RSS, disk and index size, and quota cost, for every row of the section 14 budget table except the authority load row (TASK-129.7)
- [x] #3 Each budget is marked met, or revised in plan section 14 with the measurement and rationale. The 100,000-summary failure boundary is recorded
- [x] #4 A combined scenario (rate limiting, a hung adapter, a large graph, and a full refresh at once) shows that input never freezes
- [x] #5 A CI benchmark job or test tracks relative regressions without absolute thresholds, and a `mise` task runs the full benchmark locally
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 `mise run lint`, `mise run format-check`, `mise run test`, `mise run typecheck`, and `mise run hooks` pass
- [x] #2 Any decision (D1-D27) or plan section this work contradicts or refines is updated in docs/work-queue-tui-proposal.md in the same commit
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Reuse deterministic Backlog and fake GitHub fixtures to build reproducible 10k/50k/100k queue benchmark harness with controlled latency and counters. 2. Measure section 14 rows on the D22 machine, including combined fault/refresh scenario and failure boundary; capture p50/p95/p99 and resource/quota evidence. 3. Add local mise runner and relative-only CI regression coverage. 4. Update section 14 with results and budget decisions; run all quality gates, commit, merge and verify.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Initial D22 benchmark harness on task-129-6-benchmarks: fixed 10k/50k/100k in-process TUI fixtures, 10k GitHub GraphQL fake with 5 ms/page and 100 requests, 10k SQLite FTS search. 20-sample first run exposed p95 147 ms navigation at 10k and 4,958 ms at 100k; implemented cached sorted TUI projection. Follow-up warm benchmark p95: 0.419 ms (10k), 1.321 ms (50k), 2.333 ms (100k), 60.29 ms warm first view; GitHub 619.46 ms / 1,252,057 bytes / 100 requests; index search 54.10 ms (3 samples). These are Go Update+View, not terminal paint. Continue with end-to-end, combined faults, per-row resource accounting, and section 14 decision before completion.

Checkpoint 2026-09-23: branch task-129-6-benchmarks in .worktrees/task-129-6-benchmarks, commit b3d355a (bench fixtures, p50/p95/p99 runner, relative-only PR comparison, cached TUI rows). General self-review: confirmed old TUI projection incurred repeat full sort; fixed and reran focused checks; no second general review. Verified mise run lint, format-check, test, typecheck, hooks; no acceptance criterion checked because full end-to-end budget and concurrent fault evidence outstanding. Next: benchmark cold remote page-to-render and real terminal paint, Backlog summary/edge process and bytes, measure combined rate-limit/hung adapter/large graph/full-refresh input and renewal; quantify per-row RSS/quota then decide section 14 revisions and failure boundary. Do not merge or clean worktree until complete.

Checkpoint 2026-09-24: branch task-129-6-finish in .worktrees/task-129-6-finish (created from task-129-6-benchmarks), commit 09ee0ba. Added reproducible 10k Backlog summary/edge CLI benchmark to full mise runner and CI relative report. D22 20-sample list p50/p95/p99 2133/2420/2476 ms, edge 1878/2109/2181 ms; one child per call, 5,486,861 / 4,216 output bytes, 327 MB peak child RSS, zero remote quota. Fixture 1.58 MB; docs/queue-benchmark-results.md records limits. Self-reviewed runner and corrected macOS temporary path and top-level peak RSS accounting. Verified mise run lint, format-check, test, typecheck, hooks; auto-generated fixture smoke passed. No acceptance criterion fully met; remaining cold remote page-to-render, physical terminal paint, combined faults with renewal margin, GitHub quota costs, standalone RSS and 100k failure boundary, section 14 decisions. Next resume task-129-6-finish; do not merge until complete.

Checkpoint 2026-09-24 (refresh event loop): commit 255b759 on task-129-6-finish adds 10k/50k/100k full-snapshot publication+View benchmark to local and CI-relative runner. D22 20-sample p50/p95/p99: 54.20/56.20/57.28 ms (10k), 359.80/377.27/380.33 ms (50k), 813.49/840.41/852.23 ms (100k). Update runs on the Bubble Tea input loop, so the concurrent-refresh 50 ms target currently FAILS at 50k, independent of warm keypress speed; this is not terminal input-to-paint. Verified focused 20-sample benchmark, 2-sample full mise runner, lint, format-check, test, typecheck, hooks. No new general review (previous pass recorded); targeted review confirms benchmark records stall without claiming end-to-end safety. Next: move full-refresh clone/projection work off the input event loop or revise section 14 with measured rationale; then benchmark actual terminal input/paint and combined fault scenario, remaining cold remote, RSS/quota/failure boundary. Do not merge or clean worktree until acceptance is complete.

Checkpoint 2026-09-24 (standalone RSS): branch task-129-6-complete in .worktrees/task-129-6-complete, commit 266aae8 (based on task-129-6-finish). Added process-isolated 50k/100k five-source memory + first View + refresh benchmark to local runner and CI relative report. D22 20 fresh process samples: 50k p95 first View 361.37 ms, refresh 381.93 ms, peak RSS 450,887,680 bytes; 100k p95 first View 810.86 ms, refresh 878.42 ms, peak RSS 855,064,576 bytes. 256 MiB provisional 50k memory budget fails; proposal §14 records provisional 512 MiB revision pending full concurrency. 100k failure boundary is synchronous refresh input lag (~878 ms p95) and RSS growth, not a crash. Fixed Linux ru_maxrss byte conversion. Verified two-sample full runner, 20-sample standalone, self-relative CI comparison, lint, format-check, test, typecheck, hooks. Previous general review already completed; this slice had a targeted check of process-only measurement and units, no second general pass. Still open: physical terminal input-to-paint, cold remote first-page-to-render, combined rate-limit/hung adapter/large graph/full-refresh with renewal, GitHub quota, per-row end-to-end resource data. Keep in progress; do not merge or clean worktree until acceptance complete. Next: remove refresh work from Bubble Tea input loop without losing claim-overlay freshness, then measure combined faults and terminal paint.

Checkpoint 2026-09-24 (first-page proxy): branch task-129-6-finalize in .worktrees/task-129-6-finalize, commit f6906cd (based on task-129-6-complete). Added a deterministic real GitHub-adapter first-100-summary page fetch + Go View benchmark with 5 ms injected latency to local and CI-relative runner. D22 20-sample p50/p95/p99 7.218/7.558/7.639 ms; one data request, 12,124 response bytes. This excludes authentication, index publication and actual terminal paint, so cold remote §14 acceptance remains open. Verified focused 20 samples, one-sample full mise runner, lint, format-check, test, typecheck, hooks; prior general review pass already done, no second pass. No acceptance item is complete. Next: fix synchronous full-refresh input lag, measure physical terminal input-to-paint and combined rate-limit/hung-adapter/large-graph/full-refresh with renewal, then assess end-to-end cold remote, GitHub quota, per-row accounting and §14 decisions. Keep in progress; do not merge or clean worktree until complete.

Checkpoint 2026-09-24 (refresh attribution): branch task-129-6-loop-1bc1a93f in .worktrees/task-129-6-loop-1bc1a93f, based on task-129-6-finalize and merged main at ac3ae1b. Commit d6bd80d adds deterministic 10k/50k/100k clone-vs-projection microbenchmarks to the local runner and CI relative comparison. D22 three five-iteration runs: clone about 4/25/55 ms, projection about 48/346/795 ms, allocating about 46/261/525 MB per projection; see docs/queue-benchmark-results.md. Verified focused benchmark, two-sample full runner, lint, format-check, test, typecheck, hooks. A transient queueindex multi-process test failure on first full run passed on focused and subsequent full runs. No further general review (previous pass recorded); targeted check of benchmark phases and report. No acceptance criterion completed: projection on the Bubble Tea event loop still violates the 50 ms concurrent refresh budget and combined no-freeze criterion. Next: move projection and snapshot clone off the input event loop while preserving fresh claim overlay and anchored selection, then measure actual terminal paint and combined faults/renewal; finish remaining end-to-end accounting and §14 decisions. Keep branch/worktree; do not merge or clean until complete.

Checkpoint 2026-09-24 (fixture/CI acceptance): verified existing deterministic 10k Backlog fixture, 10k fake GitHub pagination at 5 ms/page, five-source 50k/100k fixtures, and CI base/head relative comparison without absolute gates by running QUEUE_BENCH_SAMPLES=1 mise run queue-benchmark and comparing the report to itself (16 benchmark entries, two standalone sizes, 0% deltas). Acceptance #1 and #5 checked with this direct runner evidence. Full performance acceptance remains open: snapshot publication still stalls input (~384 ms at 50k in this run), and actual terminal paint, combined faults, quota, per-row accounting remain unmeasured. No code changed in this checkpoint; continue on task-129-6-loop-1bc1a93f; no merge/cleanup until remaining acceptance is met.

Checkpoint 2026-09-24 (fixture quota accounting): branch task-129-6-loop-1bc1a93f, commit b59a5b3. Fake GitHub GraphQL 10k enumeration now injects and records one quota point per data page; full benchmark JSON exposes fixture-quota-points/op (100), alongside 100 requests and 1,256,057 bytes in one-sample run. This is deterministic accounting only, NOT a measured production GitHub GraphQL charge. Focused enumeration and full one-sample mise runner passed; lint, format-check, test, typecheck, hooks passed. No new general review; narrow metric addition inspected for fixture-vs-production distinction. Remaining acceptance #2-4: actual input-to-terminal-paint, cold remote page to paint, production quota and concurrent resource accounting, combined rate-limit/hung adapter/large graph/full refresh with renewal, and section 14 final budget decisions. Most urgent: move synchronous snapshot projection off Bubble Tea input loop (50k refresh ~380 ms p95); then measure combined no-freeze. Keep in progress, do not merge or clean worktree until complete.

Checkpoint 2026-09-24 (production quota probe): commit 8d774f9 on task-129-6-loop-1bc1a93f records an authenticated github.com GraphQL query matching the adapter summary selection: microsoft/vscode returned 100 issues and rateLimit.cost=1 (remaining 4955); brettinternet/worklease returned 3 and cost=1. This validates the fake fixture per-page charge for sampled pages, not 100-page aggregate or secondary limit; docs/queue-benchmark-results.md states that boundary. Lint, format-check, test, typecheck, hooks passed. Earlier general review stands; narrow documentation evidence reviewed, no second general review. Acceptance #2-4 remain incomplete. Next: decouple snapshot clone/projection from the Bubble Tea input event loop without stale claim overlay or lost selection, measure actual terminal paint and combined faults/renewal, then fill all §14 resource rows. Keep in progress, no merge or cleanup until complete.

Checkpoint 2026-09-24 (prepared snapshots): commit dd4b69e on task-129-6-loop-1bc1a93f clones producer snapshots on worker before sending to Bubble Tea; test confirms producer isolation and preservation of newer claim observations. New local/CI-relative prepared refresh benchmark measured five 50k D22 samples p50/p95/p99 366.60/379.71/379.71 ms (Update+View; no terminal paint). The input loop still projects/sorts synchronously (~261 MB allocated), so 50 ms refresh and combined no-freeze criteria remain unmet. One-sample full mise benchmark, lint, format-check, test, typecheck, hooks passed. Narrow change reviewed; prior general review stands. Next: move sorted projection off event loop (include overlays and selection), then physical terminal paint and combined faults with renewal, cold remote and per-row resource accounting; do not merge/cleanup yet.

Checkpoint 2026-09-24 (worker-ordered keys): commit c286be9 on task-129-6-loop-1bc1a93f pre-sorts source-visible item keys on the producer worker. Event loop filters/deduplicates current snapshot items so newer claim overlays and selection remain correct. Prepared 50k Update+View three five-iteration D22 runs averaged 78.62, 97.25, 84.04 ms/op (~49 MB/op), down from 366-380 ms p95 baseline, but still above 50 ms and not physical terminal paint. Focused parity/overlay tests, lint, format-check, full test, typecheck, hooks and one-sample full queue benchmark passed. Prior general review stands; narrow check of ordering/filter/dedup/claim semantics only. Acceptance #2-4 and integration remain open; next profile and remove input-loop full-map claim merge/count/filter scans, then measure physical terminal paint, combined faults/renewal and per-row resources. Do not merge/clean until acceptance is complete.

Checkpoint 2026-09-24 (event-loop phase attribution): commit 098865e on task-129-6-loop-1bc1a93f adds a prepared-refresh Update vs cached View microbenchmark (worker preparation excluded). D22 five-iteration means for 10k/50k/100k: Update 13.64/97.01/214.56 ms, cached View 0.32/1.19/2.05 ms, Update allocations ~12/60/119 MB. docs/queue-benchmark-results.md records the method and diagnostic boundary. No general re-review; narrow benchmark/report check only. Focused benchmark, lint, format-check, full test, typecheck and hooks passed. Acceptance #2-4 remain open: Update still blocks the input loop >50 ms at 50k; physical terminal paint, combined faults/renewal, per-row resource accounting and section 14 decisions not yet complete. Next: remove full-map claim reconciliation, filtering/counting and anchoring from input-loop Update without losing overlay freshness or selection, then measure real paint and combined fault scenario. Keep worktree; do not merge or clean until acceptance complete.

Checkpoint 2026-09-24 (single-pass counts): commit 970adc7 on task-129-6-loop-1bc1a93f counts standard views and freshness/edges in one scan; configured views retain prior semantics. Two 8-iteration 50k prepared Update means were 71.61 and 70.18 ms/op (~55.6 MB/op), down from prior 97.01 ms/op, still above the 50 ms concurrent-refresh target. Added count parity test; focused benchmark, lint, format-check, full test, typecheck, hooks and commit hook passed. Narrow review of standard vs configured count semantics; prior general review stands. Acceptance #2-4 remain open. Next: remove full-map claim reconciliation/filtering/anchoring from input-loop Update without stale overlay or lost selection, then measure physical paint, combined faults with renewal, cold remote and per-row resources. Do not merge or clean worktree until acceptance complete.

Checkpoint 2026-09-24 (prepared All rows): commit 0515240 on task-129-6-loop-1bc1a93f prepares the default All row projection on the producer worker; snapshot Update patches newer claim observations, keeps source-order guard, and falls back to current-snapshot filtering for configured views. 20 D22 samples of prepared 50k Update+View p50/p95/p99 35.56/38.96/43.02 ms; standalone prepared 50k refresh+View p95 37.25 ms and peak RSS p95 362,987,520 bytes, but 20 one-shot Update-only samples p95 81.41 ms under a different GC/setup profile. 100k standalone prepared refresh p95 83.66 ms and RSS p95 716,914,688 bytes. Bench results and proposal section 14 updated, no budget declared met. Added overlay/source-order regression test; one-sample full and 20-sample full benchmark, lint, format-check, test, typecheck, hooks passed. Narrow behavior review only; prior general review stands. Acceptance #2-4 still open: physical terminal paint, cold remote first-page to terminal, combined rate-limit/hung-adapter/large-graph/full-refresh with renewal, provider/index concurrent resources and section 14 decisions. Next measure actual terminal paint and combined fault scenario, then close per-row accounting; do not merge or clean worktree until complete.

Checkpoint 2026-09-24 (PTY renderer proxy): branch task-129-6-loop-76edc502 in .worktrees/task-129-6-loop-76edc502, commit 480cbe1. Added opt-in real Bubble Tea PTY input-to-renderer-output probe. D22 20-sample 10k/50k/100k navigation p95 17.59/17.49/17.34 ms; worker-prepared full-refresh publication followed by key and distinct refreshed output p95 11.95/48.51/87.50 ms. This captures bytes emitted to PTY, NOT physical terminal display paint, nor provider/index work; message and key ordering is not guaranteed. Documented measurement boundary. Narrow review of marker freshness and opt-in harness; earlier general review stands. Focused 20-sample probe, lint, format-check, test, typecheck, hooks passed. Acceptance #2-4 remain open: physical paint, cold remote to display, combined faults and renewal, provider/index concurrency resources and complete section 14 decisions. Next: add combined fault/refresh fixture and measure renewal margin; then physical paint and remaining per-row accounting. No merge or cleanup until complete.

Checkpoint 2026-09-24 (combined fixture): commit aa354ea on task-129-6-loop-76edc502 adds opt-in 50k-summary real PTY renderer/local Worklease renewal probe under concurrent simulated rate-limited source, stalled read and 50k dependency graph recomputation plus prepared full refresh. D22 20-sample publication+input-to-PTY-output p50/p95/p99 40.24/52.93/53.43 ms; local renewal p50/p95/p99 1.19/1.27/1.45 ms, minimum prior-lease margin 9m59.35s at 10m TTL. Simulated faults are not real provider transport/backoff; PTY output is not physical paint, and p95 exceeds 50 ms. Acceptance #2-4 remain open. Focused 20-sample probe, lint, format-check, test, typecheck, hooks passed; narrow fixture review only (earlier general review stands). Next: instrument real provider/index concurrency, terminal paint and cold remote page to display, fill all §14 resource rows; do not merge or clean until acceptance complete.

Checkpoint 2026-09-24 (real adapter faults): commit dc572f0 on task-129-6-loop-76edc502 adds a fake HTTP 429/Retry-After through the actual GitHub adapter quota gate and a separate in-flight adapter HTTP request canceled after the combined PTY/refresh/graph/local-renewal probe. D22 20-sample publication+input-to-PTY-output p50/p95/p99 39.36/48.93/50.12 ms; local renewal 0.76/1.18/1.19 ms; minimum previous-lease margin 9m59.26s (10m TTL). Exactly one rate-limited and one stalled HTTP request; later limited reads made no new request. Verified focused probe, lint, format-check, full test, typecheck, hooks; narrow check of HTTP fixture, no second general review. Acceptance #2-4 still open: physical terminal paint, cold remote end-to-end, real index/provider concurrent resource accounting and final section 14 decisions; fake HTTP and PTY bytes do not prove the budgets. Next instrument production index refresh alongside actual provider reads and terminal paint, fill per-row accounting; keep worktree and branch, do not merge or clean until complete.

Checkpoint 2026-09-24 (real index contention): commit 25f1626 on task-129-6-loop-76edc502 adds 10k-summary production SQLite index Replace concurrently with the 50k PTY refresh, GitHub HTTP 429/stalled read, graph recomputation and local renewal. D22 20 samples: publication+input-to-PTY-output p50/p95/p99 49.26/56.09/56.63 ms; index Replace 21.63/23.41/23.57 s, 26.88 MB SQLite files; renewal 1.16/3.22/73.47 ms and minimum previous-lease margin 9m36.39s. Go test process peak RSS 1.118 GB includes all fixtures, not standalone TUI. One 429 and one stalled HTTP request; 60s retry window in the initial long run caused repeats, corrected fixture to 3600s and reran successfully. Full lint, format-check, test, typecheck, doc-test, hooks pass; narrow review of scope/limits only, earlier general pass stands. Acceptance #2-4 still open: production provider/index scheduler, physical terminal paint, cold remote end-to-end and per-row accounting and final section 14 decisions. Next instrument real scheduler refresh and cold page-to-terminal, or explicitly revise budgets with measured rationale; do not merge or clean until criteria complete.

Checkpoint 2026-09-24 (cold page PTY): commit 5124593 on task-129-6-loop-76edc502 adds opt-in real GitHub adapter first-page-to-live Bubble Tea PTY-output probe. D22 20 samples: request-to-output p50/p95/p99 16.65/17.50/17.56 ms; page-return-to-output 10.46/11.29/11.33 ms; one 100-summary request/sample (11,824 bytes), no full enumeration. Authentication and startup are outside timing, and PTY bytes are not physical display paint. Focused 20-sample test, lint, format-check, test, typecheck and hooks passed. Narrow review checked unique marker and page cursor; previous general review stands. Acceptance #2-4 remain open: real terminal paint, production index/provider scheduler and concurrent resources, final section 14 decisions. Next instrument production provider-to-index refresh and physical display, then per-row accounting. Do not merge or clean until complete.

Completion 2026-09-24: ec19ed5 merged fast-forward into main with all earlier benchmark commits. D22 20-sample full runner adds standalone 10k/50k/100k p50/p95/p99 and RSS and corrects per-benchmark child RSS attribution; §14 workload table records process/API/bytes/quota/disk scope, revisions and 100k refresh failure boundary. 20-sample PTY tests measured 10k navigation p95 17.95 ms and cold first-page arrival-to-output p95 11.25 ms; prior combined 50k real-adapter HTTP 429/stall + graph + SQLite Replace + renewal measured p95 56.09 ms output and minimum 9m36s previous-lease margin (10m TTL). Revises 50k concurrent-refresh target to 75 ms p95; physical terminal pixels, production provider-to-index scheduler, aggregate GitHub quota and remote renewal remain explicitly unmeasured, not claimed as guarantees. Earlier general review stands; narrow diff/metrics check only. Verified full 20-sample mise queue-benchmark, opt-in PTY probes, lint, format-check, test, typecheck, doc-test, staged hooks and commit hooks. No push.

Post-completion review: configured views now use worker-prepared projection (50k configured Update+View p95 ~29 ms), claim preview re-sorts prepared order (57a6571).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Measured and documented §14 queue budgets and fixture resource costs; added 10k standalone memory and complete benchmark-child RSS accounting, revised 50k concurrent refresh budget to 75 ms with measured rationale, verified combined input/renewal progress, and merged ec19ed5 into main. Physical paint and production concurrency are explicitly outside measured fixture guarantees.
<!-- SECTION:FINAL_SUMMARY:END -->
