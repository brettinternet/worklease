{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://worklease.dev/schemas/v1/index.json",
  "title": "Worklease schema version 1",
  "description": "Published response schemas for the worklease Python, CLI, and optional MCP compatibility surfaces.",
  "type": "object",
  "required": ["version", "common", "cli", "mcp", "commands", "leaseFile", "history"],
  "properties": {
    "version": {"const": 1},
    "common": {"const": "common.json"},
    "cli": {"const": "commands.json"},
    "mcp": {"const": "mcp.json"},
    "leaseFile": {"const": "lease-file.json"},
    "history": {"const": "history.json"},
    "commands": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "parse", "version", "instructions", "key", "policy-list", "policy-describe",
          "acquire", "bundle-acquire", "acquire-bundle",
          "status", "status-verbose", "history", "bundle-status", "inspect-bundle", "status-bundle",
          "inspect-operation", "inspect-operation-bundle",
          "reconcile-operation", "reconcile-operation-bundle", "list", "heartbeat",
          "checkpoint", "transfer", "release", "bundle-heartbeat", "heartbeat-bundle",
          "bundle-release", "release-bundle", "exec", "bundle-exec",
          "exec-bundle", "replace-file", "gc"
        ]
      }
    }
  },
  "additionalProperties": false
}
