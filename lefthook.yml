pre-commit:
  parallel: true
  commands:
    gofmt:
      glob: "*.go"
      run: test -z "$(gofmt -l {staged_files})"
    test:
      glob: ["*.go", "go.mod", "go.sum"]
      run: env -u GIT_INDEX_FILE mise run test
